//
//  Stampede_MobileTests.swift
//  Stampede-MobileTests
//
//  Created by David House on 10/11/19.
//  Copyright © 2019 David House. All rights reserved.
//

import XCTest
import SwiftUI
@testable import Stampede

class Stampede_Tests: XCTestCase {

    override func setUp() {
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }

    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
//        let host = UIHostingController(rootView: MonitorQueuesView_Previews.previews)
//        UIApplication.shared.keyWindow!.rootViewController = host
//        UIGraphicsBeginImageContextWithOptions(host.view.bounds.size, host.view.isOpaque, 0)
//        host.view.drawHierarchy(in: host.view.bounds, afterScreenUpdates: true)
//        let snapshotImage: UIImage = UIGraphicsGetImageFromCurrentImageContext()!
//        UIGraphicsEndImageContext()
//
//        let attachment = XCTAttachment(image: snapshotImage)
//        attachment.name = "RepositoriesCellView_Previews"
//        attachment.lifetime = .keepAlways
//        add(attachment)
    }    
}
